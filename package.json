{
  "name": "iris-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @iris/protocol && npm run build -w @iris/core && npm run build -w @iris/electron && npm run build -w @iris/tauri && npm run build -w @iris/mcp && npm run build -w @iris/std && npm run build -w @iris/react && npm run build -w @iris/devtools && npm run build -w @iris/test-utils",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "jsdom": "^27.2.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.6",
    "vitest": "^4.0.15",
    "zod": "^4.1.13"
  }
}
